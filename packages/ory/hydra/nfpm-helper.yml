name: hydra
download:
  url_template: https://github.com/ory/hydra/releases/download/v${VERSION}/${NAME}_${VERSION}-linux_${ARCH}.tar.gz
outputs:
  - arch: amd64
    download:
      env:
        ARCH: 64bit
  - arch: arm64
